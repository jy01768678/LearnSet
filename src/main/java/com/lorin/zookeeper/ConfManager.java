package com.lorin.zookeeper;

/**
 * 本机服务配置管理
 * 
 */
public class ConfManager {
	
	/**
	 * 注册本系统提供的服务
	 */
	public void regServices() {
//		// 读本地注册文件
//		for (int i = 0; i < array.length; i++) {
//			ConfSerCli.getConfSerCli().regServ(system, service, providerIP);
//		}
	}
	
	/**
	 * 转载本系统需要引用的服务
	 */
	public void loadServices() {
//		// 读本地注册文件
//		for (int i = 0; i < array.length; i++) {
//			ConfSerCli.getConfSerCli().updateServProvider(system, service);
//		}
	}
}
